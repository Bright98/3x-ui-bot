services:
  3x-ui-bot-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 3x-ui-bot
    restart: always
    environment:
      - BOT_TOKEN
      - SERVER_IP
      - PANEL_PORT
      - PANEL_USERNAME
      - PANEL_PASSWORD
    network_mode: host
